<!-- Licensed under the MIT License. -->

<Page
    x:Class="FIP.App.Views.CreateCustomIconPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FIP.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <Grid Margin="0 16 0 0" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Padding="8" CornerRadius="{StaticResource ControlCornerRadius}" 
                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <ColorPicker x:Name="mainColorPicker"
                                          ColorSpectrumShape="Box"
                                          IsAlphaEnabled="True"
                                          IsHexInputVisible="True" />
                </Border>
                
                <Border Grid.Column="1" Padding="8" CornerRadius="{StaticResource ControlCornerRadius}" 
                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <StackPanel Grid.Row="1" Spacing="16" Orientation="Vertical">
                        <TextBox Header="Enter folder icon name:" PlaceholderText="Name"
                                 VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                        <AutoSuggestBox Header="Select category for icon:" PlaceholderText="Name"
                                        VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                        <Button Content="Create folder icon"
                                VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <Button.Background>
                                <SolidColorBrush Color="{x:Bind mainColorPicker.Color, Mode=OneWay}"/>
                            </Button.Background>
                        </Button>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Padding="8" CornerRadius="{StaticResource ControlCornerRadius}" 
                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <Image Source="/Assets/logo.png" Margin="0 -48"/>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
