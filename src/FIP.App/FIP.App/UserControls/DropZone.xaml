<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="FIP.App.UserControls.DropZone"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FIP.App.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="DropZoneControl"
    mc:Ignorable="d">
    <Border x:Name="MainBorder" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            Padding="8"
            MinWidth="150" MaxWidth="170"
            MinHeight="100" MaxHeight="120">
        <Button x:Name="ZoneButton" Background="{Binding ElementName=DropZoneControl, Path=Color}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AllowDrop="True"
                Click="ZoneButton_Click" Drop="ZoneButton_Drop" DragOver="ZoneButton_DragOver">
            <TextBlock x:Name="ZoneTextBlock">
                
            </TextBlock>
        </Button>
    </Border>
    
</UserControl>
